kind: Service
apiVersion: v1
metadata:
  name:  movie-analist-ui-service 
  labels:
    app: movie-analyst-ui
    tier: frontend
spec:
  selector:
    app:  movie-analist-ui
    tier: frontend
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 3030
  type:  LoadBalancer

