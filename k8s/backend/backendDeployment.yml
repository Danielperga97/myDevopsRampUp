apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: movie-analyst-api
  template:
    metadata:
      labels:
        app: movie-analyst-api
    spec:
      containers:
      - name: gcr.io/ramp-up-247818/movie-analyst-api:0.1.0
        image: node:current-alpine
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 125m
            memory: 120Mi
        ports:
        - containerPort: 3000